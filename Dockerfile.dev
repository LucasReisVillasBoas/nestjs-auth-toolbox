FROM node:20-alpine
WORKDIR /app

COPY package.json package-lock.json tsconfig.json ./
RUN npm ci

ENV NODE_ENV=development

EXPOSE 3001

# src/ é montado via volume pelo docker-compose.override.yml.
# nest start --watch usa ts-node e processa TypeScript diretamente.
CMD ["npm", "run", "start:dev"]
